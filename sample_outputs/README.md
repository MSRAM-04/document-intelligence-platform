# Sample outputs

Run `python scripts/process_dataset.py` from the project root after installing the dependencies and Tesseract. The script processes every supplied invoice and financial statement with the same service used by the API and writes one structured JSON result per source file here. Do not hand-edit the generated results.
